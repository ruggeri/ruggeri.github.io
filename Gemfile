# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "slim", "~> 4.1"

# Must brew install fswatch.

# Must also brew install pandoc.
gem "pandoc-ruby", "~> 2.1"
